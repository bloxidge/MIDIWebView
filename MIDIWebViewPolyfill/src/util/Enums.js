
export const MIDIPortDeviceState = Object.freeze({
    disconnected : "disconnected",
    connected    : "connected"
});

export const MIDIPortConnectionState = Object.freeze({
    open    : "open",
    closed  : "closed",
    pending : "pending"
});
