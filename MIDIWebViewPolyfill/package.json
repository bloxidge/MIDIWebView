{
  "name": "midiwebviewpolyfill",
  "version": "0.0.0",
  "description": "Javascript polyfill for WebMIDI API, made to work with MIDIWebView Swift package.",
  "main": "index.js",
  "author": "Peter Bloxidge",
  "license": "ISC",
  "babel": {},
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^1.12.4",
    "rimraf": "^3.0.0"
  },
  "scripts": {
    "build": "run-s private:clean private:build",
    "private:build": "NODE_ENV=development parcel build index.js --out-file MIDIWebViewPolyfill.js --no-source-maps --no-minify",
    "private:clean": "rimraf dist"
  },
  "dependencies": {
    "typescript": "^3.6.4"
  }
}
